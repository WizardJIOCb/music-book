<!-- Scroll-to-top button -->
<button class="rb-scrolltop" id="rbScrollTop" type="button" aria-label="Наверх" title="Наверх">
  <!-- Орнаментальная стрелка вверх (SVG, тонкий штрих) -->
  <svg class="rb-scrolltop__svg" viewBox="0 0 96 96" width="44" height="44" aria-hidden="true">
    <!-- внешний “медальон” -->
    <path class="rb-stroke"
      d="M48 10c12 0 22 5 29 12s12 17 12 29-5 22-12 29-17 12-29 12-22-5-29-12S7 63 7 51 12 29 19 22 36 10 48 10Z"
      fill="none" />
    <!-- лёгкие “листья” сверху -->
    <path class="rb-stroke"
      d="M25 21c8 2 13 2 18-2m10 0c5 4 10 4 18 2"
      fill="none" />
    <path class="rb-stroke"
      d="M22 26c4-6 8-8 14-8m24 0c6 0 10 2 14 8"
      fill="none" />
    <!-- стрелка -->
    <path class="rb-stroke"
      d="M48 70V35"
      fill="none" />
    <path class="rb-stroke"
      d="M36 46l12-12 12 12"
      fill="none" />
    <!-- завитки снизу -->
    <path class="rb-stroke"
      d="M30 78c-6 0-10 3-10 7 0 3 2 5 5 5 4 0 6-3 5-6m36-6c6 0 10 3 10 7 0 3-2 5-5 5-4 0-6-3-5-6"
      fill="none" />
  </svg>
</button>

<style>
  :root{
    /* серый “как в оригинале” (можешь подстроить) */
    --rb-ink: #6b6b6b;
    --rb-ink-hover: #8a8a8a;
    --rb-shadow: rgba(0,0,0,.35);
  }

  .rb-scrolltop{
    position: fixed;
    right: 22px;
    bottom: 22px;
    z-index: 9999;

    display: grid;
    place-items: center;

    width: 56px;
    height: 56px;

    background: transparent;       /* важно: прозрачный фон */
    border: 0;
    padding: 0;
    cursor: pointer;

    opacity: 0;
    transform: translateY(12px) scale(.98);
    pointer-events: none;

    filter: drop-shadow(0 8px 18px var(--rb-shadow));
    transition: opacity .22s ease, transform .22s ease, filter .22s ease;
  }

  .rb-scrolltop.is-visible{
    opacity: 1;
    transform: translateY(0) scale(1);
    pointer-events: auto;
  }

  .rb-scrolltop__svg{
    display: block;
  }

  .rb-stroke{
    stroke: var(--rb-ink);
    stroke-width: 2.2;         /* тонкость линий */
    stroke-linecap: round;
    stroke-linejoin: round;
    vector-effect: non-scaling-stroke;
  }

  .rb-scrolltop:hover .rb-stroke{
    stroke: var(--rb-ink-hover);
  }

  .rb-scrolltop:focus-visible{
    outline: none;
    /* деликатный “ореол”, но не фон */
    filter: drop-shadow(0 10px 22px rgba(0,0,0,.45));
  }

  @media (prefers-reduced-motion: reduce){
    .rb-scrolltop{ transition: none; }
  }
</style>

<script>
  (function(){
    const btn = document.getElementById('rbScrollTop');
    if(!btn) return;

    const SHOW_AFTER = 220; // px — когда показывать (подстрой)

    let ticking = false;
    function onScroll(){
      if(ticking) return;
      ticking = true;
      requestAnimationFrame(() => {
        const show = window.scrollY > SHOW_AFTER;
        btn.classList.toggle('is-visible', show);
        ticking = false;
      });
    }

    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll();

    btn.addEventListener('click', () => {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });
  })();
</script>